console.log("Hello bhai jaan");

// <>hello boss</>
// The above code is wrapped inside a parent element called Fragment or any other valid HTML element, used in app.jsx file.
// ------------------------------------------------------------
// UI updation ko HOOKS control karta hai react mein.
// ------------------------------------------------------------
// Hydration occurs when React attaches event listeners to the markup generated on the server. Sometimes it happens that html page loads and the buttons are'nt working properly. This is because the event listeners are not attached to the buttons. This is called hydration.
// -------------------------------------------------------------
// devui.io is used for taking components in tailwindcss